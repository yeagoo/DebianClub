# 归档说明（ARCHIVE）

本目录是旧 VitePress 站点的内容，**已于 2026-08 冻结为只读归档**。

- 现行站点在 `web/`（Next.js + Fumadocs），内容的唯一来源是 `web/content/docs/`。
- 本目录**不再更新**，与现行内容可能存在差异（已知部分页面已漂移）。
- 请勿把本目录的内容当作最新事实引用；如需最新内容，以 `web/content/docs/` 为准。
- 根 `package.json` 的 VitePress 脚本与依赖已随冻结一并移除。
